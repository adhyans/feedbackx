import styled from "styled-components";

export const InputWithIconWrapper = styled.div`
  display: flex;
  position: relative;
  align-items: center;
  width: 20rem;

  .icon {
    position: absolute;
    margin-left: 0.8rem;
  }

  input {
    padding-left: 3.4rem;
    font-size: 1.6rem;
  }
`;
